import styled from "styled-components";

export const RedesEstilizadas = styled.div`

    display: flex;
    justify-content: center;
    align-items: center;
    margin-top: 2em;
    
    a {
        color: var(--preto);
        transition: all .2s;
    }

    a:hover {
        opacity: .7;
    }

`
