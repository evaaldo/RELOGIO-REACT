import styled from "styled-components"

export const RelogioEstilizado = styled.main`

    color: var(--branco);
    display: flex;
    align-items: center;
    gap: 5em;

    div {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    span {
        font-size: 3em;
    }

    p {
        font-size: 1.5em;
    }

`
